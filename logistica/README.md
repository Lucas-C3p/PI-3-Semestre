# logistica

Projeto Spring Boot mínimo com login/cadastro usando MongoDB.

## Como rodar

1. Tenha o Java 17+ e Maven instalados.
2. Tenha o MongoDB rodando (ex: `mongod`).
3. Na pasta do projeto rode:
   ```
   mvn spring-boot:run
   ```
4. Abra http://localhost:8080
